apiVersion: v1
kind: Service
metadata:
    name: cm-tracking-almacenar-pin-guia
    namespace: apis
spec:
    ports:
        - name: http
          port: 8080
          protocol: TCP
    selector:
        app: cm-tracking-almacenar-pin-guia
